# Local dev requirements only.
# On SOFY, do not install this file; use tools/setup_sofy_qwen_env.sh so the
# container-provided /opt/ac2 torch stack stays untouched.

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
accelerate>=0.24.0
safetensors>=0.4.0

# Diffusers - may need to install from source for latest QwenImage support
# pip install git+https://github.com/huggingface/diffusers.git
diffusers>=0.25.0

# CLI and utilities
typer>=0.9.0
pillow>=10.0.0
tqdm>=4.66.0
tensorboard>=2.16.0

# Optional but recommended
# numpy>=1.24.0
# scipy>=1.10.0
